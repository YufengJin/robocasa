version: "3.9"

services:
  robocasa:
    container_name: robocasa_container
    image: ${IMAGE:-robocasa:latest}
    build:
      context: ..
      dockerfile: docker/Dockerfile
    network_mode: host
    volumes:
      - ../:/workspace
      # - /home/yjin/repos/robocasa/datasets:/workspace/datasets
    working_dir: /workspace
    stdin_open: true
    tty: true
    environment:
      - GPU=${GPU:-all}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
